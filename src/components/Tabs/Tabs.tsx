import { FC } from "react";
import style from "./Tabs.module.scss";

const Tabs: FC = () => {
  return (
    <div className={style["tabs"]}>
      <h2>Tabs</h2>
    </div>
  );
};

export default Tabs;
